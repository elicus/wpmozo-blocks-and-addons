jQuery( document ).ready( function($) {
	// Each elements loop.
	$( '.wp-block-wpmozo-blog-timeline' ).each( function() {
		wpmozo_init_blog_timeline( $( this ) );
	} );

} ); // Document ready.

/**
 * Init facebook embedded video.
 */
function wpmozo_init_blog_timeline( thisObj ) {

}
