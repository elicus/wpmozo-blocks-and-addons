jQuery( document ).ready( function($) {
	// Each elements loop.
	$( '.wp-block-wpmozo-background-switcher' ).each( function() {
		
	} );

	// On change on props, update the slider again.
	window.addEventListener( 'WPMozoBackgroundSwitcherPropsChanged', (e) => {
		
	} );
} ); // Document ready.
